---
// Hero Section - Above the fold
---

<section id="hero" class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-violet-50 via-white to-cyan-50">
  <!-- Animated AI Background - CSS & SVG only -->
  <div class="absolute inset-0 z-0">
    <!-- SVG Animated Network -->
    <svg class="absolute inset-0 w-full h-full" xmlns="http://www.w3.org/2000/svg">
      <defs>
        <!-- Gradientes para las líneas -->
        <linearGradient id="lineGrad1" x1="0%" y1="0%" x2="100%" y2="100%">
          <stop offset="0%" style="stop-color:#8b5cf6;stop-opacity:0.4" />
          <stop offset="100%" style="stop-color:#06b6d4;stop-opacity:0.4" />
        </linearGradient>
        <linearGradient id="lineGrad2" x1="100%" y1="0%" x2="0%" y2="100%">
          <stop offset="0%" style="stop-color:#06b6d4;stop-opacity:0.3" />
          <stop offset="100%" style="stop-color:#8b5cf6;stop-opacity:0.3" />
        </linearGradient>

        <!-- Filtro de brillo -->
        <filter id="glow">
          <feGaussianBlur stdDeviation="3" result="coloredBlur"/>
          <feMerge>
            <feMergeNode in="coloredBlur"/>
            <feMergeNode in="SourceGraphic"/>
          </feMerge>
        </filter>
      </defs>

      <!-- Líneas animadas conectando nodos -->
      <g class="ai-lines">
        <line x1="10%" y1="20%" x2="30%" y2="40%" stroke="url(#lineGrad1)" stroke-width="2" class="line-1" />
        <line x1="30%" y1="40%" x2="50%" y2="30%" stroke="url(#lineGrad1)" stroke-width="2" class="line-2" />
        <line x1="50%" y1="30%" x2="70%" y2="50%" stroke="url(#lineGrad2)" stroke-width="2" class="line-3" />
        <line x1="70%" y1="50%" x2="90%" y2="35%" stroke="url(#lineGrad2)" stroke-width="2" class="line-4" />

        <line x1="15%" y1="70%" x2="40%" y2="65%" stroke="url(#lineGrad1)" stroke-width="2" class="line-5" />
        <line x1="40%" y1="65%" x2="60%" y2="75%" stroke="url(#lineGrad2)" stroke-width="2" class="line-6" />
        <line x1="60%" y1="75%" x2="85%" y2="70%" stroke="url(#lineGrad1)" stroke-width="2" class="line-7" />

        <line x1="20%" y1="50%" x2="45%" y2="55%" stroke="url(#lineGrad2)" stroke-width="1.5" class="line-8" />
        <line x1="55%" y1="60%" x2="75%" y2="45%" stroke="url(#lineGrad1)" stroke-width="1.5" class="line-9" />
      </g>

      <!-- Nodos brillantes en las intersecciones -->
      <g class="ai-nodes" filter="url(#glow)">
        <circle cx="10%" cy="20%" r="4" fill="#8b5cf6" class="node node-1" />
        <circle cx="30%" cy="40%" r="5" fill="#06b6d4" class="node node-2" />
        <circle cx="50%" cy="30%" r="4" fill="#8b5cf6" class="node node-3" />
        <circle cx="70%" cy="50%" r="5" fill="#06b6d4" class="node node-4" />
        <circle cx="90%" cy="35%" r="4" fill="#8b5cf6" class="node node-5" />

        <circle cx="15%" cy="70%" r="4" fill="#06b6d4" class="node node-6" />
        <circle cx="40%" cy="65%" r="4" fill="#8b5cf6" class="node node-7" />
        <circle cx="60%" cy="75%" r="5" fill="#06b6d4" class="node node-8" />
        <circle cx="85%" cy="70%" r="4" fill="#8b5cf6" class="node node-9" />

        <circle cx="20%" cy="50%" r="3" fill="#06b6d4" class="node node-10" />
        <circle cx="45%" cy="55%" r="3" fill="#8b5cf6" class="node node-11" />
        <circle cx="55%" cy="60%" r="4" fill="#06b6d4" class="node node-12" />
        <circle cx="75%" cy="45%" r="4" fill="#8b5cf6" class="node node-13" />
      </g>

      <!-- Partículas flotantes pequeñas -->
      <g class="ai-particles">
        <circle cx="25%" cy="25%" r="1.5" fill="#8b5cf6" opacity="0.6" class="particle particle-1" />
        <circle cx="65%" cy="35%" r="1" fill="#06b6d4" opacity="0.5" class="particle particle-2" />
        <circle cx="35%" cy="80%" r="1.5" fill="#8b5cf6" opacity="0.4" class="particle particle-3" />
        <circle cx="80%" cy="60%" r="1" fill="#06b6d4" opacity="0.6" class="particle particle-4" />
        <circle cx="50%" cy="45%" r="1.5" fill="#8b5cf6" opacity="0.5" class="particle particle-5" />
        <circle cx="15%" cy="55%" r="1" fill="#06b6d4" opacity="0.4" class="particle particle-6" />
      </g>
    </svg>

    <!-- Gradient blobs animados con CSS -->
    <div class="absolute inset-0">
      <div class="absolute top-20 left-10 w-96 h-96 bg-violet-400 rounded-full mix-blend-multiply filter blur-3xl opacity-20 animate-blob"></div>
      <div class="absolute top-40 right-10 w-96 h-96 bg-cyan-400 rounded-full mix-blend-multiply filter blur-3xl opacity-20 animate-blob animation-delay-2000"></div>
      <div class="absolute bottom-20 left-1/2 w-96 h-96 bg-purple-300 rounded-full mix-blend-multiply filter blur-3xl opacity-20 animate-blob animation-delay-4000"></div>
    </div>
  </div>

  <!-- Overlay con gradiente para profundidad -->
  <div class="absolute inset-0 bg-gradient-to-b from-transparent via-white/80 to-white z-10"></div>

  <!-- Contenido principal -->
  <div class="relative z-20 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
    <div class="text-center">
      <!-- Headline -->
      <h1 class="text-5xl sm:text-6xl lg:text-7xl font-bold mb-6 bg-gradient-to-r from-violet-600 to-cyan-500 bg-clip-text text-transparent leading-tight">
        El asistente con IA que entiende búsquedas y muestra propiedades
      </h1>

      <!-- Subheadline -->
      <p class="text-xl sm:text-2xl text-gray-700 mb-12 max-w-3xl mx-auto font-light">
        Respondé consultas reales, filtrá automáticamente tu catálogo y capturá leads calificados. Sin intervención manual, las 24 horas.
      </p>

      <!-- CTA principal -->
      <a
        href="#demo"
        class="inline-block px-8 py-4 bg-gradient-to-r from-violet-600 to-violet-700 text-white text-lg font-semibold rounded-full hover:shadow-2xl hover:shadow-violet-500/50 transition-all duration-300 hover:scale-105"
      >
        Probar el asistente en vivo
      </a>

      <!-- Mock del Chat - Screenshot Real -->
      <div class="mt-16 max-w-2xl mx-auto">
        <img
          src="/mock-chat-belgrano.png"
          alt="Demo del chat InmoBot mostrando búsqueda de departamento en Belgrano"
          class="w-full rounded-2xl shadow-2xl border border-gray-200"
        />
      </div>
    </div>
  </div>
</section>

<style>
  /* Optimized CSS animations - GPU accelerated, smooth 60fps */

  /* Gradient blobs movement */
  @keyframes blob {
    0%, 100% {
      transform: translate3d(0, 0, 0) scale(1);
    }
    33% {
      transform: translate3d(30px, -50px, 0) scale(1.1);
    }
    66% {
      transform: translate3d(-20px, 20px, 0) scale(0.9);
    }
  }

  .animate-blob {
    animation: blob 20s ease-in-out infinite;
    will-change: transform;
  }

  .animation-delay-2000 {
    animation-delay: 2s;
  }

  .animation-delay-4000 {
    animation-delay: 4s;
  }

  /* SVG Nodes pulse animation */
  @keyframes nodePulse {
    0%, 100% {
      opacity: 0.6;
      transform: scale(1);
    }
    50% {
      opacity: 1;
      transform: scale(1.2);
    }
  }

  .node {
    animation: nodePulse 3s ease-in-out infinite;
  }

  .node-1 { animation-delay: 0s; }
  .node-2 { animation-delay: 0.2s; }
  .node-3 { animation-delay: 0.4s; }
  .node-4 { animation-delay: 0.6s; }
  .node-5 { animation-delay: 0.8s; }
  .node-6 { animation-delay: 1s; }
  .node-7 { animation-delay: 1.2s; }
  .node-8 { animation-delay: 1.4s; }
  .node-9 { animation-delay: 1.6s; }
  .node-10 { animation-delay: 1.8s; }
  .node-11 { animation-delay: 2s; }
  .node-12 { animation-delay: 2.2s; }
  .node-13 { animation-delay: 2.4s; }

  /* Lines flow animation */
  @keyframes lineFlow {
    0% {
      stroke-dashoffset: 1000;
    }
    100% {
      stroke-dashoffset: 0;
    }
  }

  .ai-lines line {
    stroke-dasharray: 500;
    stroke-dashoffset: 1000;
    animation: lineFlow 8s linear infinite;
  }

  .line-1 { animation-delay: 0s; }
  .line-2 { animation-delay: 0.5s; }
  .line-3 { animation-delay: 1s; }
  .line-4 { animation-delay: 1.5s; }
  .line-5 { animation-delay: 2s; }
  .line-6 { animation-delay: 2.5s; }
  .line-7 { animation-delay: 3s; }
  .line-8 { animation-delay: 3.5s; }
  .line-9 { animation-delay: 4s; }

  /* Particles floating animation */
  @keyframes particleFloat {
    0%, 100% {
      transform: translate3d(0, 0, 0);
      opacity: 0.4;
    }
    25% {
      transform: translate3d(10px, -15px, 0);
      opacity: 0.8;
    }
    50% {
      transform: translate3d(-5px, -30px, 0);
      opacity: 0.6;
    }
    75% {
      transform: translate3d(-15px, -15px, 0);
      opacity: 0.7;
    }
  }

  .particle {
    animation: particleFloat 12s ease-in-out infinite;
  }

  .particle-1 { animation-delay: 0s; }
  .particle-2 { animation-delay: 2s; }
  .particle-3 { animation-delay: 4s; }
  .particle-4 { animation-delay: 6s; }
  .particle-5 { animation-delay: 8s; }
  .particle-6 { animation-delay: 10s; }

  /* Performance optimization */
  .ai-lines,
  .ai-nodes,
  .ai-particles {
    will-change: auto; /* Let browser optimize */
  }

  /* Reduce motion for accessibility */
  @media (prefers-reduced-motion: reduce) {
    .animate-blob,
    .node,
    .ai-lines line,
    .particle {
      animation: none !important;
    }
  }
</style>
